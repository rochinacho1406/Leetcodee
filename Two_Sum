#TWO SUM CODE (LEET CODE)
class Solution(object):
    def twoSum(self, nums, target):
        seen = {}
        for i in range(len(nums)):
            num = nums[i]
            num2= target - num
            if num2 in seen:
                return [seen[num2], i]
            seen[num] = i
